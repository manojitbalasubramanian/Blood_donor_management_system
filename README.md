step1:
setup ".env" file with help of ".env.example" 

step2:
use "npm i" to install the dependency

step2:
"npm run frontend" or "cd ./frontend && npm run dev"
"npm run backend" or "cd ./backend && npm run start"
